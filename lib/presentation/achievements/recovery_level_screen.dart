import 'package:flutter/material.dart';

class RecoveryLevelScreen extends StatelessWidget {
  const RecoveryLevelScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(body: Center(child: Text('Recovery Level Screen')));
  }
}
