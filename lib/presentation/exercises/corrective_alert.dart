import 'package:flutter/material.dart';

class CorrectiveAlertScreen extends StatelessWidget {
  const CorrectiveAlertScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(body: Center(child: Text('Corrective Alert HUD')));
  }
}
